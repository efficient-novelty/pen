import PEN.CAD.Atoms

namespace PEN.Acceptance.ReferenceAssets

open PEN.CAD

/-- Acceptance-only placeholder for reference telescope assets.
    Lane 1 (`discover_blind`) must not import this module. -/
def referenceTelescope : List AtomicDecl := []

/-- Acceptance-only placeholder canonical lookup asset.
    Lane 1 (`discover_blind`) must not import this module. -/
def canonicalLookup : List String := []

end PEN.Acceptance.ReferenceAssets
